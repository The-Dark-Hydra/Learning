public class Main {
    public static void main(String[] args) {

        System.out.print("im trying to learn java\n");
        // so here we can use /n to break the line and point to be noted that the /n should be inside the double quote
        // now i will try to use data types to learn java

        int Math = 20;
        double decimal = 20.2;
        char name = 'H';
        boolean Handsome = false;
        String naam = "Hydra";

        System.out.println(naam + " score in exam was " + Math + " ,the class average was " + decimal + ", hydra is quite handsome " + Handsome);

        if (Handsome == true) {
            System.out.println("hydra is a handsome guy");
        } else {
            System.out.println("hydra is not a handsome guy");
        }
    }
}